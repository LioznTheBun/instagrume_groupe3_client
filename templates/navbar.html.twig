<!-- Navbar -->
<nav
	class="navbar navbar-expand-lg navbar-dark bg-dark">
	<!-- Container wrapper -->
	<div
		class="container">
		<!-- Navbar brand -->
		<a class="navbar-brand" href="#"><img id="Instagrume-logo" src="{{ asset('images/logo.svg') }}" alt="Instagrume logo" draggable="false" height="70"/><span>Instagrume</span></a>

		<!-- Toggle button -->
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>

		<!-- Collapsible wrapper -->
		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<!-- Left links -->
			<ul class="navbar-nav">
				<li class="nav-item">
					<a class="nav-link active d-flex me-3 align-items-center" aria-current="page" href="/">Accueil</a>
				</li>
			</ul>
			<!-- Left links -->

			<form class="d-flex align-items-center w-100 form-search">
				<input type="search" class="form-control" placeholder="Pseudo à chercher" aria-label="Search"/>
				<a href="#!" class="text-white">
					<i class="fas fa-search ps-3"></i>
				</a>
			</form>

			<ul class="navbar-nav">
				{% if role != NULL %}
					<li class="nav-item me-3">
						<a class="nav-link d-flex align-items-center {{ page == 'profil' ? 'active' :'' }}" href="/myself">Profil</a>
					</li>
				{% endif %}
				{% if role == 'admin' %}
					<li class="nav-item">
						<a class="nav-link d-flex align-items-center me-3 {{ page == 'utilisateurs' ? 'active' :'' }}" href="/users">Utilisateurs</a>
					</li>
				{% endif %}
				{% if role == NULL %}
					<li class="nav-item">
						<a class="nav-link d-flex align-items-center" href="/login">Connexion/Inscription</a>
					</li>
				{% endif %}
				{% if role != NULL %}
					<li class="nav-item">
						<a class="nav-link d-flex align-items-center" href="/logout">Déconnexion</a>
					</li>
				{% endif %}
			</ul>
		</div>
		<!-- Collapsible wrapper -->
	</div>
	<!-- Container wrapper -->
</nav>
<!-- Navbar -->
